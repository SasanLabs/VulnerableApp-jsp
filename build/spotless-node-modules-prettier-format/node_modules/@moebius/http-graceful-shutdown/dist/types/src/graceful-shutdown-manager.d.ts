/// <reference types="node" />
import { Server as HttpServer } from 'http';
import { Server as HttpsServer } from 'https';
export declare class GracefulShutdownManager {
    private server;
    private connections;
    private nextConnectionId;
    private terminating;
    constructor(server: HttpServer | HttpsServer);
    terminate(callback: () => void): void;
    private startWatchingServer();
    private onConnection(connection);
    private onRequest(request, response);
    private closeIdleConnection(connection);
}
